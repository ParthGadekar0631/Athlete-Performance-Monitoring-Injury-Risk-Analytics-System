[project]
name = "baseball-athlete-performance-analytics"
version = "0.1.0"
description = "Baseball athlete workload, readiness, and injury-risk analytics simulation."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
